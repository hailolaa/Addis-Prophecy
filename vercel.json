{
    "framework": null,
    "buildCommand": null,
    "outputDirectory": "public",
    "installCommand": "pip install -r requirements.txt",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}